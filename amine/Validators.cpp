#include "Configuration.hpp"

ServerConfig	Configuration::validate_listen(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_index(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_server_name(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_allowed_methods(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_root(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_error_page(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_location(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_clients(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_autoindex(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_upload_store(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_upload_enable(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Configuration::validate_return(const Directive &listen) {(void)listen; return (ServerConfig());}
