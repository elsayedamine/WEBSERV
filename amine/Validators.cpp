#include "Configuration.hpp"

ServerConfig	Config::validate_listen(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_index(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_server_name(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_allowed_methods(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_root(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_error_page(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_location(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_clients(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_autoindex(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_upload_store(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_upload_enable(const Directive &listen) {(void)listen; return (ServerConfig());}

ServerConfig	Config::validate_return(const Directive &listen) {(void)listen; return (ServerConfig());}
